---
const { nomMaison, prix, surface, adresse, imgUrl, favoris } = Astro.props;
---

<div class="offre" data-favori={favoris}>
    <img src={imgUrl} alt={nomMaison} />
    <h2>{nomMaison}</h2>
    <p>{adresse}</p>
    <p>{surface} m² — {prix} €</p>
</div>
